spring.application.name=kremlin
# spring.jpa.show-sql=true
# spring.datasource.url=jdbc:h2:mem:mibase
# spring.h2.console.enabled=true
logging.level.org.hibernate.SQL=INFO
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# URL de conexión
spring.datasource.url=jdbc:mysql://localhost:3306/kremlin_db?useSSL=false&serverTimezone=America/Argentina/Buenos_Aires

# Credenciales
spring.datasource.username=root
spring.datasource.password=Pepe_1272

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Codificación (opcional pero recomendable)
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci;

# JWT
app.jwt.secret=d7464f36c7785f9387117b87a6fc160a4e9f4da25af907d9a583acde68380588
app.jwt.expiration-ms:14400000

# Session cookie
# (DEV -> lax / PROD -> none)
server.servlet.session.cookie.same-site=lax
# (DEV -> false / PROD -> true)
server.servlet.session.cookie.secure=false